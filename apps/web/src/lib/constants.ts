/**
 * Public app-wide constants.
 * These are not sensitive — no need for environment variables.
 */

export const SUPPORT_EMAIL = 'support@stagelink.art';
export const SUPPORT_URL = `mailto:${SUPPORT_EMAIL}`;
