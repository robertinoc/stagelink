/**
 * Public app-wide constants.
 * These are not sensitive — no need for environment variables.
 */

// TODO (infra): migrate to support@stagelink.art once Cloudflare Email Routing is configured
export const SUPPORT_EMAIL = 'stagelink.qa@gmail.com';
export const SUPPORT_URL = `mailto:${SUPPORT_EMAIL}`;
