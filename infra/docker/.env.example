# Docker Compose overrides — copy to .env in this directory to customize
# Defaults work out of the box for local development.

POSTGRES_USER=stagelink
POSTGRES_PASSWORD=stagelink
POSTGRES_DB=stagelink_dev

MINIO_ROOT_USER=stagelink
MINIO_ROOT_PASSWORD=stagelink123
